# СТЕРЕОЭФФЕКТ В ФОТОГРАММЕТРИИ
## Полный конспект лекции с практическими примерами

---

## ЧАСТЬ 1: ОСНОВЫ И ИСТОРИЯ

### 1.1 Почему нас интересует стереоэффект?

**Вопрос:** Почему мы можем видеть глубину двумя глазами? Что происходит в мозге?

**Ответ:** Бинокулярное зрение — это система, основанная на сравнении двух немного отличающихся изображений. Каждый глаз видит мир с чуть другого угла.

**Пример:** Закройте левый глаз и указательный палец на фоне комнаты. Затем закройте правый глаз. Палец «прыгнул» на несколько сантиметров! Это *параллакс* — фундаментальное явление.

**Применение в фотограмметрии:**
- Две камеры на самолёте или дроне снимают местность одновременно
- Получается стереопара — два снимка с чуть смещёнными объектами
- Благодаря смещению (параллаксу) мы вычисляем высоту каждой точки
- Так создаются карты, модели рельефа, 3D модели зданий

### 1.2 Краткая история (зачем нужно знать?)

| Год | Событие | Значение |
|-----|---------|----------|
| 1725 | М.А. Каппелер рисует две панорамы горы Пилатус | **Рождение пространственной засечки** |
| 1852 | Э. Лосседа выполняет первую фото съёмку для плана | Начало фотограмметрии (назвал её *метрофотография*) |
| 1892 | Ф. Штольц предлагает способ мнимой марки | **Главное изобретение** — используется 130 лет! |
| 1901 | К. Пулфрих создаёт стереокомпаратор | Оптико-механический прибор высокой точности |
| 1957–1960 | Ю.В. Хелава: первый *аналитический плоттер* (АП-1) | Начало компьютерной эры |
| 1990-е | Полный переход на цифровые методы | Современная фотограмметрия родилась |
| 2020-е | Нейросети + классические алгоритмы | Future is hybrid |

**Главный вывод:** То, что вручную делали 150 лет, теперь автоматизировано — но *принципы остались прежними*.

---

## ЧАСТЬ 2: ФУНДАМЕНТАЛЬНЫЕ КОНЦЕПЦИИ

### 2.1 Что такое стереопара?

**Определение:** Пара фотоснимков одного объекта, полученных из **разных точек** в пространстве.

```
Левая камера S_L              Правая камера S_R
    ↑                             ↑
    └─────────────────────────────┘
              Базис B (10 см – 100 м)
    
    ↓ снимки →
    
   ┌──────────────┐           ┌──────────────┐
   │   Левый      │           │   Правый     │
   │  снимок      │           │  снимок      │
   │              │           │              │
   │    • P_1     │           │     • P_1    │  ← одна точка
   │   (x_L, y)   │           │    (x_R, y)  │  но разные координаты x!
   └──────────────┘           └──────────────┘
```

**Условия валидной стереопары:**

1. **Масштаб:** различие масштабов ≤ 16%
   - Если одна камера дальше, снимок будет меньше
   - Пример: если m₁ = 1:1000, то m₂ может быть 1:840–1:1200

2. **Конвергенция:** угол между оптическими осями ≤ 15°
   - Камеры смотрят примерно в одну сторону
   - Не рекомендуется "косить" в разные стороны

3. **Перекрытие:** хотя бы 50% общей площади
   - Каждая точка объекта должна видна в обоих снимках

**Антипример (не является стереопарой):**
```
Снимок 1: вид спереди       Снимок 2: вид сбоку
    (ракурс 0°)              (ракурс 90°)
    
    ✓ Можно составить 3D      ✓ Но это не стереопара
    ✗ Слишком большая         ✗ Конвергенция > 15°
      конвергенция
```

### 2.2 Стереомодель и её масштаб

**Определение:** Трёхмерное изображение объекта, возникающее при стереоскопическом наблюдении стереопары.

**Ключевое свойство:** Масштаб глубины *отличается* от масштаба в плане!

**Формула масштаба глубины:**

$$m_z = m_{xy} \cdot \frac{250}{f}$$

где:
- $m_{xy}$ = масштаб в плане (например, 1:1000)
- $f$ = фокусное расстояние в миллиметрах
- 250 = стереомодульный коэффициент (константа)

**Численный пример:**

- Дано: $m_{xy} = 1:1000$, фокус $f = 100$ мм
- Вычисляем: $m_z = \frac{1}{1000} \cdot \frac{250}{100} = \frac{1}{1000} \cdot 2.5 = \frac{1}{400}$
- **Интерпретация:** Вертикальный масштаб 1:400, плановый 1:1000
  - Холмик высотой 50 м в натуре → 50 м ÷ 400 = 0.125 мм на модели
  - Но в горизонтальном направлении этот же холм → 0.05 мм
  - **Результат:** холм выглядит 2.5 раза выше, чем в реальности!

**Практическое применение:**
- Топографы специально выбирают параметры, чтобы рельеф хорошо виделся
- Если $m_z = m_{xy}$, рельеф кажется плоским (ортогональное проектирование)
- Если $m_z \ll m_{xy}$, рельеф выглядит преувеличено (идеально для анализа)

### 2.3 Параллакс — король стереоскопии

**Определение:** Смещение изображения точки между левым и правым снимками.

```
Левый снимок          Правый снимок
      
      ↓ на этой позиции
      X ← точка P
      x_L              x_R ← та же точка, но здесь
      
Параллакс p = x_L - x_R  ← всегда > 0 для точек впереди
```

**Основная формула (ЗАПОМНИТЬ!):**

$$Z = \frac{f \times B}{p}$$

где:
- $Z$ = глубина точки (расстояние от камер до объекта)
- $f$ = фокусное расстояние (в пикселях!)
- $B$ = базис между камерами (в метрах)
- $p$ = параллакс (в пикселях)

**Численные примеры:**

| f (px) | B (см) | p (px) | Z (м) | Интерпретация |
|--------|--------|--------|-------|---------------|
| 3000 | 10 | 10 | **30** | Объект рядом (10 см базиса → 30 м глубины) |
| 3000 | 10 | 30 | **10** | Объект дальше (большой параллакс → меньше Z) |
| 3000 | 10 | 100 | **3** | Объект очень далеко |

**Расчёт на доске (пошагово):**

Задача: Два дрона летят на высоте H = 100 м, расстояние между ними B = 20 м. Фокус камеры f = 4000 px. Объект на земле виден с параллаксом p = 50 px. На какой высоте h над землёй летят дроны?

*Решение:*
1. Вычислим Z (расстояние от дронов до объекта): $Z = \frac{4000 \times 20}{50} = \frac{80000}{50} = 1600$ м
2. Но дроны на 100 м. Высота объекта: $h = 1600 - 100 = 1500$ м... нет, стоп!
3. Нужно уточнить геометрию (см. Краснопевцев, гл. 5.7)

**Качественный анализ:**
- Маленький параллакс → дальний объект ($Z = \frac{f \times B}{p}$ большое при маленьком $p$)
- Большой параллакс → ближний объект
- Если базис $B$ увеличить в 2 раза → $Z$ увеличится в 2 раза (при фиксированном $p$)

### 2.4 Точность определения глубины

**Ошибка в глубине связана с ошибкой в параллаксе:**

$$\sigma_Z = \frac{f \times B}{p^2} \times \sigma_p$$

**Интерпретация:**
- Если ошибка параллакса $\sigma_p = 1$ px в 10 раз больше, чем при 0.1 px
- То ошибка в глубине $\sigma_Z$ возрастает в 100 раз (!!)
- **Вывод:** Точность параллакса критична. Нужен точный стереоматчинг

**Пример:**
- $f = 3000$ px, $B = 100$ см, $p = 30$ px
- $Z = \frac{3000 \times 1}{30} = 100$ м
- Если $\sigma_p = 0.5$ px: $\sigma_Z = \frac{3000 \times 1}{30^2} \times 0.5 = \frac{3000}{900} \times 0.5 = 1.67$ м
- **Относительная точность:** 1.67 м из 100 м = 1.67% ✓ хорошо!

---

## ЧАСТЬ 3: ЭПИПОЛЯРНАЯ ГЕОМЕТРИЯ

### 3.1 Зачем нужна эпиполярная геометрия?

**Проблема:** Когда я нашёл соответствующий пиксель в левом снимке, где его искать в правом?

- **Наивный способ:** Искать по всей полосе → медленно (O(n²))
- **Правильный способ:** Искать только на *эпиполярной линии* → быстро (O(n))

**Эпипол (E):** Проекция одного центра камеры на другой снимок.

```
Геометрия:
         S_L ←────── S_R
          ↑            ↑
          │            e_R
          │          (эпипол на правом снимке)
    Левый снимок
```

### 3.2 Выпрямление (Rectification)

**Идея:** Повернуть оба снимка так, чтобы эпиполярные линии стали горизонтальными.

```
До выпрямления          После выпрямления
Точка на диагонали      Точка на горизонтальной строке
    X ╱                     X ──────
    ╱X                      ── X ──
╱ X                            ──X
```

**Результат после выпрямления:**
$$y_L = y_R \text{ (одна и та же координата y)}$$

Это означает: если точка находится на строке 150 левого снимка, её соответствие найдётся на строке 150 правого снимка!

**Математика (не пугайтесь!):**
- Нужно вычислить фундаментальную матрицу $\mathbf{F}$
- Из $\mathbf{F}$ извлечь матрицы выпрямления $\mathbf{R}_L$, $\mathbf{R}_R$
- Применить проективное преобразование к каждому снимку

**В OpenCV это одна функция:**
```python
R1, R2, P1, P2, Q = cv2.stereoRectify(K_L, d_L, K_R, d_R, 
                                       img_size, R, t)
```

---

## ЧАСТЬ 4: АЛГОРИТМЫ СТЕРЕОМАТЧИНГА

### 4.1 Постановка задачи

**Дано:**
- Выпрямленная левая и правая стереопара
- Каждый пиксель левого снимка $I_L(x, y)$

**Найти:**
- Соответствующий пиксель в правом снимке $I_R(x - d, y)$
- Параллакс $d$ для каждого $(x, y)$

**Результат:** Карта диспарности (disparity map) — изображение, где каждый пиксель кодирует параллакс.

```python
# На выходе получаем такое изображение:
disparity = [
    [10, 12, 11, 15, ...],    # строка 0
    [11, 13, 10, 16, ...],    # строка 1
    [12, 14, 11, 17, ...],    # строка 2
]
# Тёмнее = ближе (большой параллакс)
# Светлее = дальше (маленький параллакс)
```

### 4.2 Block Matching (BM) — интуитивный метод

**Идея:** Для каждого пикселя слева найти **похожее** окно справа.

**Шаг 1: Выберем окно слева (15×15 пиксель)**
```
Левый снимок:
       ┌─────────────┐
       │             │
       │  ┌────────┐ │
       │  │ 15×15  │ │
       │  │ окно   │ │
       │  └────────┘ │
       └─────────────┘
```

**Шаг 2: Будем сдвигать такое же окно по правому снимку**
```
Правый снимок:
       ┌─────────────┐
       │             │
 смещение← d →
       │  ┌────────┐ │
       │  │ 15×15  │ │ ← левое положение
       │  └────────┘ │
       │  ┌────────┐ │
       │  │ 15×15  │ │ ← правое положение (смещено на d)
       │  └────────┘ │
       └─────────────┘
```

**Шаг 3: Вычислим меру сходства (Sum of Absolute Differences, SAD)**

$$\text{SAD}(d) = \sum_{i,j} |I_L(x+i, y+j) - I_R(x+i-d, y+j)|$$

где окно $i, j \in [-7, +7]$ для размера 15×15.

**Шаг 4: Найдём параллакс, при котором SAD минимален**

```python
min_cost = float('inf')
best_d = 0

for d in range(0, 80):  # ищем диспарность от 0 до 80 пиксель
    cost = calculate_SAD(left_window, right_window_shifted_by_d)
    if cost < min_cost:
        min_cost = cost
        best_d = d

disparity[x, y] = best_d
```

**Параметры StereoBM:**

| Параметр | Значение | Эффект |
|----------|----------|--------|
| `blockSize` | 15 (нечётное) | Размер окна. Меньше → шум, больше → смазано |
| `numDisparities` | 80 | Максимальный параллакс. Если объект дальше → нужно увеличить |
| `minDisparity` | 0 | Минимальный параллакс (обычно 0) |

**Плюсы:** Простая, быстрая (~100 мс), интуитивная.

**Минусы:** 
- Шумные результаты
- Плохо работает на текстурных областях (вода, небо)
- Нет учёта соседства пикселей

### 4.3 Semi-Global Matching (SGM) — оптимальный выбор

**Идея:** Добавим ограничение — диспарности соседних пикселей должны быть похожи.

**Энергетическая функция:**

$$E(\mathbf{d}) = \sum_p D(p, d_p) + \sum_{p, q \in N} R(p, d_p, q, d_q)$$

где:
- **Первый член** $D(p, d_p)$: Стоимость соответствия (SAD/SSD)
- **Второй член** $R(...)$: Штраф за разницу диспарности соседей

**Штраф за несоответствие соседей:**

$$R = \begin{cases}
0 & \text{если } |d_p - d_q| = 0 \text{ (соседи совпадают)}\\
P_1 & \text{если } |d_p - d_q| = 1 \text{ (слабый штраф)}\\
P_2 & \text{если } |d_p - d_q| > 1 \text{ (сильный штраф)}
\end{cases}$$

**Параметры:**
- $P_1 = 10$: штраф за градуальное изменение
- $P_2 = 100$: штраф за скачкообразное изменение

**Визуальное сравнение:**

```
StereoBM:                 StereoSGBM:
┌─────────────┐          ┌─────────────┐
│   ┴ ┴ ┴     │          │   ▓▓▓       │  ← гладкие регионы
│ ║ ║ ║ ║ ║   │          │ ║░░░░░░░░║  │
│ ║ ║ ║ ║ ║   │          │ ║░░░░░░░░║  │
│             │          │             │
└─────────────┘          └─────────────┘
```

**Время вычисления:**
- StereoBM: ~100 мс для 1280×720
- StereoSGBM: ~500 мс для 1280×720

**Качество:**
- StereoBM: 80% правильных пикселей (на Middlebury benchmark)
- StereoSGBM: 95%+ правильных пикселей

**Вывод:** SGM — стандартный выбор в фотограмметрии.

---

## ЧАСТЬ 5: ПРАКТИЧЕСКИЙ КОД

### 5.1 Минимальный пример

```python
import cv2
import numpy as np
from matplotlib import pyplot as plt

# 1. Загрузить выпрямленные снимки
left = cv2.imread('left_rect.jpg', cv2.IMREAD_GRAYSCALE)
right = cv2.imread('right_rect.jpg', cv2.IMREAD_GRAYSCALE)

# 2. Создать объект StereoBM
stereo = cv2.StereoBM_create(
    numDisparities=16*5,  # 80 пикселей
    blockSize=15
)

# 3. Вычислить диспарность
disparity = stereo.compute(left, right)

# 4. Вычислить глубину (главная формула!)
focal_length = 3000  # пиксели
baseline = 0.1       # 10 см в метрах
depth = (focal_length * baseline) / (disparity.astype(float) / 16.0)

# 5. Визуализировать
plt.figure(figsize=(15, 5))
plt.subplot(131)
plt.imshow(left, cmap='gray')
plt.title('Левый снимок')

plt.subplot(132)
plt.imshow(disparity, cmap='hot')
plt.title('Карта диспарности')

plt.subplot(133)
plt.imshow(depth, cmap='viridis')
plt.title('Карта глубины (метры)')

plt.tight_layout()
plt.show()
```

### 5.2 С калибровкой и выпрямлением

```python
import cv2
import numpy as np

# Параметры камер (из калибровки)
K_left = np.array([
    [3000, 0, 320],
    [0, 3000, 240],
    [0, 0, 1]
], dtype=float)

K_right = np.array([
    [3000, 0, 320],
    [0, 3000, 240],
    [0, 0, 1]
], dtype=float)

d_left = np.array([0.05, -0.1, 0, 0])  # Коэффициенты дисторсии
d_right = np.array([0.05, -0.1, 0, 0])

R = np.eye(3)      # Матрица поворота (почти единичная)
t = np.array([[-0.1], [0], [0]])  # Вектор трансляции (10 см влево)

# Размер снимка
size = (640, 480)

# Выпрямление
R1, R2, P1, P2, Q = cv2.stereoRectify(
    K_left, d_left,
    K_right, d_right,
    size,
    R, t,
    flags=cv2.CALIB_ZERO_DISPARITY,
    alpha=0
)

# Загрузить исходные (не выпрямленные) снимки
left_orig = cv2.imread('left_orig.jpg', cv2.IMREAD_GRAYSCALE)
right_orig = cv2.imread('right_orig.jpg', cv2.IMREAD_GRAYSCALE)

# Применить выпрямление (переквадрирование)
map1_left, map2_left = cv2.initUndistortRectifyMap(
    K_left, d_left, R1, P1, size, cv2.CV_32F
)
map1_right, map2_right = cv2.initUndistortRectifyMap(
    K_right, d_right, R2, P2, size, cv2.CV_32F
)

left_rect = cv2.remap(left_orig, map1_left, map2_left, cv2.INTER_LINEAR)
right_rect = cv2.remap(right_orig, map1_right, map2_right, cv2.INTER_LINEAR)

# Теперь используем выпрямленные снимки для стереоматчинга
stereo = cv2.StereoSGBM_create(
    minDisparity=0,
    numDisparities=80,
    blockSize=15,
    P1=600,        # 8*blockSize^2
    P2=2400,       # 32*blockSize^2
    disp12MaxDiff=1,
    preFilterCap=63,
    uniquenessRatio=10,
    speckleWindowSize=100,
    speckleRange=2
)

disparity = stereo.compute(left_rect, right_rect)

# Вычислить 3D координаты
points_3d = cv2.reprojectImageTo3D(disparity, Q)

print(f"Облако точек форма: {points_3d.shape}")
print(f"Min глубина: {points_3d[:,:,2].min()}")
print(f"Max глубина: {points_3d[:,:,2].max()}")
```

### 5.3 Визуализация 3D облака (Open3D)

```python
import open3d as o3d
import numpy as np

# points_3d получена из cv2.reprojectImageTo3D

# Расплющить в список точек
h, w = points_3d.shape[:2]
points = points_3d.reshape(-1, 3)

# Отфильтровать плохие точки (где диспарность = 0)
valid = ~np.any(np.isnan(points) | np.isinf(points), axis=1)
points = points[valid]

# Создать облако точек
pcd = o3d.geometry.PointCloud()
pcd.points = o3d.utility.Vector3dVector(points)

# (Опционально) Оценить нормали
pcd.estimate_normals(search_param=o3d.geometry.KDTreeSearchParamHybrid(
    radius=0.1, max_nn=30
))

# Визуализировать
o3d.visualization.draw_geometries([pcd])

# Сохранить
o3d.io.write_point_cloud("cloud.ply", pcd)
```

---

## ЧАСТЬ 6: ВЫЗОВЫ И ПУТИ РЕШЕНИЯ

### 6.1 Текстурные области

**Проблема:** На воде, небе, гладких стенах нет текстуры для матчинга.

```
Вода (однородная):    Стена:
██████████            ░░░░░░░░░░
██████████            ░░░░░░░░░░
██████████  → нет     ░░░░░░░░░░  → алгоритм не знает,
██████████    примет  ░░░░░░░░░░    где искать соответствие
```

**Результат:** На карте глубины появляются чёрные дыры (нет данных).

**Решения:**

1. **Passive Stereo (текущий подход):**
   - Использовать многовидовую стереоскопию (3+ снимков)
   - Применить постфильтр (интерполяция по соседям)

2. **Active Stereo:**
   - Добавить проектор структурированного света
   - Проецирует случайный паттерн → появляется текстура
   - Используется в Kinect, Intel RealSense

3. **Deep Learning:**
   - Обучить нейросеть на большом датасете
   - Сеть "научится" угадывать глубину и в текстурных областях

### 6.2 Окклюзии

**Проблема:** Один пиксель видна в левом снимке, но скрыта в правом.

```
Вид сверху:
    ┌────────┐
    │ дом    │
    └────────┘
         ↑
    Левая камера видит дом
    Правая камера видит дом позади
    
    → На краях дома алгоритм ошибается
```

**Решение: Left-Right Consistency Check**

```python
# Вычислить диспарность слева→вправо
disp_left_to_right = stereo.compute(left, right)

# Вычислить диспарность справа→слево
disp_right_to_left = stereo.compute(right, left)

# Проверить согласованность
consistency_check = np.abs(disp_left_to_right - 
                           (-disp_right_to_left)) < 1

# Обнулить несогласованные пиксели
disp_left_to_right[~consistency_check] = 0
```

### 6.3 Шум и артефакты

**Проблема:** Даже SGM даёт шумные результаты в некоторых областях.

**Решение: Фильтры постобработки**

```python
from cv2 import ximgproc

# Weighted Least Squares (WLS) фильтр
wls_filter = ximgproc.createDisparityWLSFilter(stereo_left)
wls_filter.setLambda(8000)
wls_filter.setSigmaColoringEdges(4)

filtered_disp = wls_filter.filter(disp_left, left)

# Медианный фильтр (простой, но эффективный)
median_disp = cv2.medianBlur(disp, 5)
```

---

## ЧАСТЬ 7: СОВРЕМЕННЫЕ ПОДХОДЫ

### 7.1 Глубокое обучение

**Класс моделей:** CNN для стереоматчинга (Cost Volume Networks)

**Примеры:**
- **CREStereo (2023):** Без plane-sweep, очень быстро
- **RAFTStereo (2021):** Итеративный рефайнмент, высокая точность
- **GCNet (2017):** Graph Cut, классический подход

**Преимущества:**
- Лучше работают на текстурных областях
- Меньше артефактов
- Можно обучить on-domain (на своих данных)

**Недостатки:**
- Требуют большой датасет для обучения
- Медленнее классических методов (но GPU ускоряет)
- Менее интерпретируемо

### 7.2 Гибридный подход (современный стандарт)

**Идея:** Комбинировать классические методы и нейросети.

```
Левый + Правый снимок
         ↓
    [OpenCV SGM] → первый результат
         ↓
   [CNN рефайнер] → улучшенный результат
         ↓
  [Постфильтр WLS] → финальный результат
         ↓
    Карта глубины ✓
```

**Производство (реальные системы):**
- **Pix4D:** SGM + их оптимизация + постфильтр
- **Hexagon LPS:** SGM + нейросеть
- **DJI (дроны):** Собственные алгоритмы (неизвестны, но быстрые)

---

## ЧАСТЬ 8: ПРИМЕНЕНИЕ В ФОТОГРАММЕТРИИ

### 8.1 Создание ЦМР (Цифровой Модели Рельефа)

**Процесс:**

1. **Аэросъёмка:** Самолёт / дрон делает снимки с перекрытием ~60%
2. **Стереопары:** Соседние снимки образуют стереопары
3. **Стереоматчинг:** Для каждой пары вычисляется карта глубины
4. **Слияние:** Все карты объединяются в одну ЦМР
5. **ГИС обработка:** Интерполяция, фильтрация, экспорт

**Результат:**
- Высокоточная модель рельефа (точность ±0.1 м при полёте на 100 м)
- Для создания топографических карт
- Для анализа эрозии, оползней, ледников

### 8.2 Ортофотокарта

**Идея:** Спроектировать снимок на плоскую карту, исправив искажения рельефа.

**Формула (упрощённо):**
$$\text{Орто}(x, y) = \text{Снимок}(u, v)$$

где $(u, v)$ вычисляются из координат $(x, y, Z)$ и параметров камеры.

**Применение:**
- Кадастровые карты
- Навигация для аграрных операций
- Анализ городской застройки

---

## ЧАСТЬ 9: ФОРМУЛЫ-ШПАРГАЛКА

**Для решения задач на экзамене:**

| Формула | Применение | Пример |
|---------|------------|--------|
| $Z = \frac{f \times B}{d}$ | Основная. Вычислить глубину | $Z = \frac{3000 \times 0.1}{30} = 10$ м |
| $d = \frac{f \times B}{Z}$ | Обратная. Найти параллакс | $d = \frac{3000 \times 0.1}{10} = 30$ px |
| $m_z = m_{xy} \cdot \frac{250}{f}$ | Масштаб глубины | $m_z = \frac{1}{1000} \times 2.5 = \frac{1}{400}$ |
| $\sigma_Z = \frac{f \times B}{d^2} \times \sigma_d$ | Ошибка глубины | $\sigma_Z = \frac{3000 \times 0.1}{900} \times 0.5 = 0.17$ м |

---

## ЧАСТЬ 10: ПОЛЕЗНЫЕ САЙТЫ И ИСТОЧНИКИ

### Код и примеры
- OpenCV docs: docs.opencv.org/master/d9/d0c/group__calib3d__stereo.html
- GitHub: github.com/spmallick/learnopencv (папка stereo/)
- Middlebury: vision.middlebury.edu/stereo/

### Видеолекции
- Cyrill Stachniss (YouTube): Structure from Motion, Dense 3D Reconstruction
- LearnOpenCV (YouTube): Stereo Vision tutorials

### Учебники (скачать)
- Hartley & Zisserman (PDF): 150 MB
- Краснопевцев (PDF): у нас на портале

---

## ЗАКЛЮЧЕНИЕ

**Три кита стереоскопии:**

1. **Параллакс** — смещение точки между снимками
2. **Формула** — Z = fB/d связывает параллакс с глубиной
3. **Стереоматчинг** — алгоритм находит параллакс автоматически

**От истории к практике:**
- 1725 г.: Рисование панорам вручную
- 1892 г.: Механический способ мнимой марки
- 1960 г.: Первый компьютерный аппарат
- 2024 г.: Нейросети + классические методы
- **2030 г.: ??** (ваш вклад!)

**Главное для экзамена:**
- ✓ Знать формулу Z = fB/d и её смысл
- ✓ Понимать, почему нужна калибровка
- ✓ Уметь запустить stereo.compute() в OpenCV
- ✓ Интерпретировать карту глубины

**Домашнее задание:**
```bash
# Установить
pip install opencv-contrib-python open3d numpy matplotlib

# Скачать Middlebury
wget https://vision.middlebury.edu/stereo/data/scenes2014/GroundTruth-Outdoor-Aloe-x10.zip

# Запустить код
python stereo_code_examples.py

# Экспериментировать с параметрами blockSize, numDisparities
# и наблюдать результат!
```

**Успехов в изучении фотограмметрии! 🚀**

